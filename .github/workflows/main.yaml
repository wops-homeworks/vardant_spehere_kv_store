name: "Main Workflow"

on:
  pull_request:
    branches:
      - dev
      - main

jobs:
  build:
    name: Build Laravel
    uses: ./.github/workflows/build.yaml
    with:
      version: "8.2"

  test:
    name: Test Laravel
    needs: [build]
    uses: ./.github/workflows/test.yaml
